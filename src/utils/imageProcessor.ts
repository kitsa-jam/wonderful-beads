export async function loadImage(file: File): Promise<HTMLImageElement> { return new Promise((resolve,reject)=>{ const img=new Image(); img.onload=()=>resolve(img); img.onerror=reject; img.src=URL.createObjectURL(file); }); }
export function sampleImage(img: HTMLImageElement, width:number, height:number, mirror='none') { const canvas=document.createElement('canvas'); canvas.width=width; canvas.height=height; const ctx=canvas.getContext('2d')!; ctx.imageSmoothingEnabled=true; if(mirror==='horizontal'||mirror==='both'){ctx.translate(width,0);ctx.scale(-1,1);} if(mirror==='vertical'||mirror==='both'){ctx.translate(0,height);ctx.scale(1,-1);} ctx.drawImage(img,0,0,width,height); const d=ctx.getImageData(0,0,width,height).data; return Array.from({length:width*height},(_,i)=>({r:d[i*4],g:d[i*4+1],b:d[i*4+2],a:d[i*4+3]})); }
