import type { PatternResult } from '../../types/pattern';
export function PatternCanvas({result}:{result:PatternResult}){ return <div className="patternWrap"><div className="axis top">{Array.from({length:result.width},(_,i)=><span key={i}>{i+1}</span>)}</div><div className="axis left">{Array.from({length:result.height},(_,i)=><span key={i}>{i+1}</span>)}</div><div className="patternGrid" style={{gridTemplateColumns:`repeat(${result.width}, minmax(18px,1fr))`}}>{result.cells.map(c=><span key={`${c.row}-${c.col}`} className="cell" style={{background:c.hex, borderRight:(c.col+1)%29===0?'2px solid #2f2f3a':'', borderBottom:(c.row+1)%29===0?'2px solid #2f2f3a':''}} title={`${c.row+1},${c.col+1} ${c.colorId}`}>{result.settings.style==='codes-grid'?c.colorId:''}</span>)}</div></div> }
