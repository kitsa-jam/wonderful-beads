import { ArrowRight, Grid2X2, Palette, Smartphone, FileDown, ShieldCheck, SlidersHorizontal } from 'lucide-react';
const features=[['自由尺寸',SlidersHorizontal],['真实色卡',Palette],['色号统计',Grid2X2],['手机可用',Smartphone],['一键导出',FileDown],['本地处理',ShieldCheck]];
export function HomePage(){ return <section className="home"><div className="hero"><div><p className="eyebrow">在线图片转拼豆图纸生成器</p><h1>玩豆否 <span>Wonderful Beads</span></h1><p>玩豆否，把喜欢拼成 wonderful。</p><p className="sub">上传一张图片，生成可打印的拼豆图纸。</p><a className="cta" href="#/create">开始玩豆 <ArrowRight size={20}/></a></div><div className="beadLogo" aria-label="Wonderful Beads logo">{Array.from({length:49},(_,i)=><i key={i}/>)}</div></div><div className="featureGrid">{features.map(([name,Icon])=><article className="card feature" key={name as string}><Icon size={24}/><b>{name as string}</b></article>)}</div></section> }
